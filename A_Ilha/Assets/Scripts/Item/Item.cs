using UnityEngine;

public class Item : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    public void ativaImagem(bool visivel)
    {
        
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
